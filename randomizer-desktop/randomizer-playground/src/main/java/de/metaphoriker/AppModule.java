package de.metaphoriker;

import com.google.inject.AbstractModule;

public class AppModule extends AbstractModule {

  @Override
  protected void configure() {
    /*
    bind(ActionSequenceRepository.class).toInstance(new ActionSequenceRepository());
    bind(ActionRegistry.class).toInstance(new ActionRegistry());
    bind(ApplicationContext.class).toInstance(new ApplicationContext());
    bind(KeyBindRepository.class).toInstance(new KeyBindRepository());
     */
  }
}
